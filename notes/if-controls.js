// If controls
let a = 0;

if (a === 0) {
    // 100% equal in terms of types and values
}

if (a == 0) {
    // 100% equal in terms and values
}

if (a !== 0) {
    // 100% not equal in terms of types and values
}

if (a != 0) {
    // 100% not equal in terms of and values
}

let isExecuted = false;

if (!isExecuted) {
    // false condition
}

let userName = undefined;

if (!userName) {
    // truthy value
}

if (!userName) {
    // falsy value
}